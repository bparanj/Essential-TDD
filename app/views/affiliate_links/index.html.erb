<fieldset>

<table>
  <tr>
    <th> Product Name</th>
    <th>Price</th>
    <th>Commission</th>
	<th></th>
  </tr>
  
<% @products.each do |p| %>
  
  <tr>
    <td><%= p.name %></td>
    <td><%= p.price %></td>
    <td><%= link_to 'Get Affiliate Link',affiliate_link_url(p) %></td>
  </tr>
  
<% end %>
</table>

<br />

</fieldset>

<%= link_to 'Back', root_path %>